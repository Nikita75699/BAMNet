\documentclass[tikz,border=15pt]{standalone}
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, calc, backgrounds}

\begin{document}

\tikzset{
    base/.style = {draw=black!70, thick, align=center, inner sep=3ex, rounded corners=5pt, font=\sffamily\large, text width=4.5cm},
    input/.style = {base, fill=blue!15, text=black},
    enc/.style = {base, fill=yellow!20, text=black!80!orange},
    dec/.style = {base, fill=green!15, text=black!60!green},
    feat/.style = {base, fill=purple!15, text=black!60!purple},
    outblock/.style = {base, fill=blue!15, text=black, font=\sffamily\bfseries\large},
    loss/.style = {base, fill=red!15, text=black!80!red, rounded corners=0pt},
    arrow/.style = {-{Latex[scale=1.5]}, thick, draw=black!70},
    skip/.style = {-{Latex[scale=1.5]}, thick, dashed, draw=red!60!black},
}

\begin{tikzpicture}[node distance=1.5cm and 2.5cm]

% --- Main Path ---
\node[input] (input) {Input Image\\$640\times 640\times 3$\\(Fluoroscopy)};
\node[enc, right=of input] (encoder) {EfficientNet-V2\\Encoder\\(5 Stages)};
\node[dec, right=of encoder] (decoder) {MA-Net\\Decoder\\(w/ Position Attention)};
\node[feat, right=of decoder] (refine) {Refinement Block\\(seg\_feats)};
\node[outblock, above=1.5cm of refine] (segout) {Segmentation Mask\\(Aortic Root)};

% --- Landmark Branch ---
\node[enc, below=2cm of refine] (lm_branch) {Landmark Localization\\Branch\\(Upsample + Fusion +\\Pointwise + Attention)};
\node[outblock, right=of lm_branch] (coords) {Landmark Coordinates\\(AA1, AA2, STJ1, STJ2)};

% --- Losses ---
\node[loss, right=of refine] (losses) {Total Loss\\(Segmentation +\\Boundary +\\Landmark Coords)};

% --- Connections ---
\begin{scope}[on background layer]
    \draw[arrow] (input) -- (encoder);
    \draw[arrow] (encoder) -- (decoder);
    \draw[arrow] (decoder) -- (refine);
    \draw[arrow] (refine) -- (segout);
    
    \draw[arrow] (encoder.south) |- (lm_branch.west);
    \draw[arrow] (refine.south) -- (lm_branch.north);
    
    \draw[arrow] (lm_branch) -- (coords);
    
    \draw[skip] (segout.east) -| (losses.north);
    \draw[skip] (coords.north) |- (losses.south);
    
    % skip connection
    \draw[skip, draw=black!50] (encoder.north) -- ++(0, 1.5) -| node[above, pos=0.25] {Skip Connections} (decoder.north);
\end{scope}

\node[above=3cm of decoder, font=\sffamily\bfseries\Large] (title) {Simplified BoundaryAwareMAnet Architecture};

\end{tikzpicture}

\end{document}
